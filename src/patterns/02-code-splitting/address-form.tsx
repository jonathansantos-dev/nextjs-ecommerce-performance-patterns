// Stub that lives in a separate file to simulate a real checkout sub-module.
// In production this file would pull in address-validation libraries (~40KB).
export { AddressForm } from './checkout-drawer';
